export class Category {
    id: string;
    name: string;
    description: string;
    constructor(id:string, name:string, desc:string){
        this.id=id;
        this.name=name;
        this.description=desc;
    }
}